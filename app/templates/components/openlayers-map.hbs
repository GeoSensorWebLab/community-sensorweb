{{#with (hash
	tile-layer=(component "openlayers-map/tile-layer" parentComponent=this)
	vector-layer=(component "openlayers-map/vector-layer" parentComponent=this)
	popup=(component "openlayers-map/popup" parentComponent=this)
) as |components|}}
	{{yield components}}
{{/with}}
